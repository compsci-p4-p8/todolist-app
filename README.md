# todolist-app
the code for the todolist app

to run the app run `uv run main.py`to run the app run `uv run main.py`to run the app run `uv run main.py`to run the app run `uv run main.py`to run the app run `uv run main.py`